Скрытые ключевые слова
  ок   __reftype для int даёт Int32
  ок   __reftype для string даёт String
  ок   __refvalue пишет в исходную переменную: 100

Переменное число аргументов
  система: Microsoft Windows 10.0.20348, X64
  ожидалось: вызов проходит
  ок   исход совпал с ожидаемым для платформы: Int32 = 1; String = два; Double = 3,5; 

Замена типа у живого объекта
  ок   во время замены массив зовётся строкой: String
  ок   после возврата массив снова массив
  ок   данные массива на месте
  ок   сборка мусора после возврата прошла

Операторы true и false
  ок   объект со значением да проходит условие
  ок   объект со значением нет не проходит

Вызов у пустой ссылки
  ок   метод с полем падает
  ок   метод без обращения к полям тоже падает
  ок   метод расширения не падает

СВЕРКА ПРОЙДЕНА
